language: generic

sudo: required
services:
  - docker

before_install:
  - docker build -t armughanahmed/docker-react -f dockerfile.dev .

script:
  - docker run -e CI=true armughanahmed/docker-react npm run test
